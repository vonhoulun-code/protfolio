# 项目规则

1. 开工前阅读 `README`、`docs/architecture.md` 和 `content/yangming-profile.md`。
2. 不编造史实、日期、引文和图片授权。
3. 每个类、组件、服务端模块、导出函数和数据表都必须在 `docs/architecture.md` 精确说明职责、输入、输出和调用关系。
4. 修改代码时同步更新相关文档，代码与文档必须在同一任务完成。
5. 不提交 SQLite 数据库文件、密钥、环境变量和联系人数据。
6. 修改导航、页面、API 或数据库后运行对应测试。
7. 不顺手重构需求外代码。
8. 未经确认，不读取项目外文件、不推送远程、不执行删除操作。
